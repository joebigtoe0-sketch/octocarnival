[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20", "vips"]
aptPkgs = ["libvips-dev"]

[phases.install]
cmds = [
  "npm install",
  "node scripts/ensure-uuid-cjs.js",
  "node scripts/bundle-sprites.js",
  "npm rebuild sharp"
]

[start]
cmd = "sh start.sh"
